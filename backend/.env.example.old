################################################################################
# CRITICAL SECURITY WARNING
################################################################################
# This is an EXAMPLE configuration file. DO NOT use these values in production.
#
# BEFORE DEPLOYING TO PRODUCTION:
# 1. Generate strong, random secrets (minimum 32 characters)
# 2. Use a secure secret management system (AWS Secrets Manager, HashiCorp Vault)
# 3. Rotate all credentials from development/testing
# 4. Never commit actual secrets to version control
# 5. Review and update all security-related configuration
#
# To generate secure secrets, use:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
################################################################################

# Application
NODE_ENV=development
PORT=3001

# Database
# SECURITY: Use strong passwords (min 16 chars, mixed case, numbers, symbols)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password_here
DB_NAME=whitecross

# JWT Authentication
# CRITICAL: Generate unique secrets for each environment
# Minimum 32 characters, use cryptographically secure random strings
# NEVER use these example values in production
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production-min-32-chars

# CSRF Protection
# CRITICAL: Required for CSRF token generation
# Must be different from JWT_SECRET
CSRF_SECRET=your-super-secret-csrf-key-change-in-production-min-32-chars

# CORS Configuration
# CRITICAL: Never use '*' in production
# For multiple origins, use comma-separated list: http://app1.com,http://app2.com
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=debug

# Redis Configuration
# Used for caching, queue management, and session storage
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_USERNAME=

# Redis Cache Configuration
REDIS_DB=0
REDIS_TTL_DEFAULT=300
REDIS_CONNECTION_TIMEOUT=5000
REDIS_MAX_RETRIES=3
REDIS_RETRY_DELAY=1000

# Redis Queue Configuration
REDIS_QUEUE_DB=1

# Cache Configuration
CACHE_KEY_PREFIX=cache
CACHE_ENABLE_COMPRESSION=false
CACHE_COMPRESSION_THRESHOLD=1024
CACHE_ENABLE_L1=true
CACHE_L1_MAX_SIZE=1000
CACHE_L1_TTL=60
CACHE_ENABLE_LOGGING=false
CACHE_WARMING_ENABLED=false

# Message Queue Configuration
QUEUE_CONCURRENCY_DELIVERY=10
QUEUE_CONCURRENCY_NOTIFICATION=15
QUEUE_CONCURRENCY_ENCRYPTION=5
QUEUE_CONCURRENCY_INDEXING=3
QUEUE_CONCURRENCY_BATCH=2
QUEUE_CONCURRENCY_CLEANUP=1

# Encryption Configuration
# CRITICAL: Used for PHI field-level encryption (HIPAA compliance)
ENCRYPTION_ALGORITHM=aes-256-gcm
ENCRYPTION_KEY=your-encryption-key-min-32-chars-change-in-production
ENCRYPTION_KEY_SIZE=32
ENCRYPTION_IV_SIZE=16
RSA_KEY_SIZE=4096
KEY_ROTATION_ENABLED=true
KEY_ROTATION_INTERVAL_DAYS=90

# Digital Signature Secret
# Used for document signatures and timestamping
SIGNATURE_SECRET=your-signature-secret-min-32-chars-change-in-production

# WebSocket Configuration
WS_PORT=3002
WS_PATH=/socket.io
WS_CORS_ORIGIN=http://localhost:3000
WS_PING_TIMEOUT=60000
WS_PING_INTERVAL=25000
