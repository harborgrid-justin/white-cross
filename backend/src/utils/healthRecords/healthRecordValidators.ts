/**
 * Health Record Validators - Re-export Module
 *
 * This module re-exports validators from the main validators directory
 * to maintain module structure compatibility within the healthRecords utils folder.
 */

export * from '../healthRecordValidators';
export { default } from '../healthRecordValidators';
