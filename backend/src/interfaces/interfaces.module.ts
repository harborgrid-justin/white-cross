import { Module } from '@nestjs/common';

/**
 * Interfaces Module
 * Provides shared TypeScript interfaces used across the application
 */
@Module({
  exports: [],
})
export class InterfacesModule {}
