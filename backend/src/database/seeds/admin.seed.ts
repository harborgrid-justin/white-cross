/**
 * Admin User Seeder (Placeholder)
 * Use the new npm run create:users script instead
 */

export async function seedAdminUser(): Promise<void> {
  console.log('🔄 seedAdminUser called - redirecting to new system');
  console.log('💡 Use: npm run create:users');
  console.log('   This will create all user types including admin');
}