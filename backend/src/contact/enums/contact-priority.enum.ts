/**
 * Contact priority levels for emergency contacts
 * Determines contact order during emergency situations
 */
export enum ContactPriority {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
  EMERGENCY_ONLY = 'EMERGENCY_ONLY',
}
