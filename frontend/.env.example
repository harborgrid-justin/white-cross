# Next.js Environment Variables Example
# Copy this file to .env.local and update with your actual values

# Primary API Host - Centralized configuration for all API calls
# This is the single source of truth for the backend API URL
# - Development: http://localhost:3001
# - Docker: http://backend:3001  
# - Production: https://api.your-domain.com
NEXT_PUBLIC_API_URL=http://localhost:3001

# Internal API URL for server-side requests (Docker environments)
# Falls back to NEXT_PUBLIC_API_URL if not set
INTERNAL_API_URL=http://localhost:3001

# Node environment
NODE_ENV=development
