/**
 * State Sync Middleware - Convenience Export
 *
 * Re-exports state synchronization middleware from the redux directory.
 * Provides a convenient top-level import path for backward compatibility.
 */

export * from './redux/stateSyncMiddleware';
