/**
 * Message search components barrel export
 */

export { MessageSearch } from './MessageSearch';
export type {
  SearchFilters,
  SavedSearch,
  SearchResult,
  MessageSearchProps,
} from './types';
export { defaultFilters, priorityOptions } from './types';
