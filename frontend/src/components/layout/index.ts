/**
 * Layout Components
 * 
 * Components that control page structure and navigation.
 */

export { default as AppLayout } from './AppLayout'

// Legacy export for backward compatibility
export { default as Layout } from './AppLayout'