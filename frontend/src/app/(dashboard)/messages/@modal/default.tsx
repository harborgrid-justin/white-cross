/**
 * Default Modal for Messages
 * 
 * This component provides the default (empty) modal state for the messages parallel route.
 * When no modal is active, this returns null to render nothing.
 */
export default function MessagesDefaultModal(): null {
  return null;
}