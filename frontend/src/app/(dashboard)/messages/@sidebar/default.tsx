import MessagesSidebar from '../_components/MessagesSidebar';

/**
 * Default Sidebar for Messages
 * 
 * This component provides the default sidebar content for the messages parallel route.
 * It includes navigation, metrics, and recent activity for healthcare messaging.
 */
export default function MessagesDefaultSidebar(): React.JSX.Element {
  return <MessagesSidebar />;
}