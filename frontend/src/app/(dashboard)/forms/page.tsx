/**
 * Forms main page - content is handled by layout.tsx with FormsContent component
 */
export default function FormsPage() {
  // Content is handled by the layout with FormsContent component
  // This page serves as the main route for /forms
  return null;
}
