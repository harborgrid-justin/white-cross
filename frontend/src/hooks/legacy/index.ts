/**
 * Legacy Hooks
 *
 * Deprecated hooks maintained for backward compatibility.
 * These hooks will be removed in a future version.
 *
 * @module hooks/legacy
 * @deprecated Use hooks from their respective domain modules instead
 * @since 3.0.0
 */

/**
 * @deprecated Use `useToast` from `@/hooks/ui` instead
 */
export * from './use-toast';

// Legacy backup files are kept for reference but not exported