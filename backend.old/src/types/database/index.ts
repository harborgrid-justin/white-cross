/**
 * Database types barrel export.
 * Central export point for database-related type definitions.
 *
 * @module types/database
 *
 * @remarks
 * Re-exports all database query result types and analytics interfaces.
 * Import from this module for database operation type safety.
 *
 * @example
 * ```typescript
 * import { VendorDeliveryMetrics, TimeSeriesQueryResult } from '@/types/database';
 * ```
 */

export * from './query-results.types';
